/**
 * WebApp PostCSS config / NEXO Institucional
 * Responsabilidad: Pipeline PostCSS para Tailwind CSS y autoprefixer en la SPA.
 * Dependencias: postcss, tailwindcss, autoprefixer.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
